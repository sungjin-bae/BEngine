if (Windows)

# Make window visual studio project


else () # Windows

# Make android studio project
 add_library( engine

                 # Sets the library as a shared library.
                 SHARED

                 # Provides a relative path to your source file(s).
                 src/main/cpp/native-lib.cpp )


endif() # Windows
